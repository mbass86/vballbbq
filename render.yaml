services:
  - type: web
    name: volleyfest-app
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    disk:
      name: vball-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 10000
